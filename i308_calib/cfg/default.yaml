video: 0
name: Default

# mode can be 'auto', 'any' or 'dshow'
# capture_mode: 'auto'

# requested resolution
# resolution: 640x480

# available resolution
#resolutions:
#  - 640x480
#  - 1280x720

# threaded, if True the captures are made in a separate thread preventing gui from block
# threaded: False
